# Weather-app
To use the application simply do the following steps:
 1.Open VS Code and create a new terminal inside the project directory.
 2.Install the needed modules using 'npm i' command.
 3. Start and run the server using 'nodemon index.js' command.
